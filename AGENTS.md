# Agent Instructions

- After any backend code update, automatically restart the backend before reporting completion.
- Restart the backend with `.\run.ps1` from the project root unless the user gives a different command.
- The backend listens on `http://127.0.0.1:8000` by default.
